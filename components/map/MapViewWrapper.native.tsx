/**
 * Native (iOS/Android): Re-export der echten MapView von react-native-maps.
 * Diese Datei wird nur auf nativen Plattformen geladen — Metro wählt sie
 * automatisch wegen der .native.tsx Endung.
 */
export { default } from 'react-native-maps';
